Simple TMC5130 (Trinamic) driver examples.

Top-level language folders:
- `python/` original Python driver example tested on Raspberry Pi
- `rust/` Rust driver scaffold based on the Python example
- `c/` C driver scaffold based on the Python example
- `cpp/` C++ driver scaffold based on the Python example
- `js/` JavaScript driver scaffold based on the Python example
